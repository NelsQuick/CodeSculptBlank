{% extends 'base.html.twig' %}

{% block title %}Mon Profil{% endblock %}

{% block body %}
    <h1>Mon Profil</h1>
    <p>Nom d'utilisateur : {{ user.username }}</p>
    <p>Nom : {{ user.name }}</p>
    <p>Adresse e-mail : {{ user.email }}</p>

<div class="profil-button-container"></div>
    <a href="{{ path('app_profile_edit_name') }}" class="profil-button">Modifier le Nom</a>
    <a href="{{ path('app_logout') }}" class="profil-button">Déconnexion</a>
{% endblock %}
